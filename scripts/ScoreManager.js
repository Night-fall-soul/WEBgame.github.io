class ScoreManager {
    constructor(dom_renderer) {
        this.dom = dom_renderer;
    }

    addScore(points) { }

    getScore() {
        //return number
    }

    reset() {
        // resets score
        // reestablece el marcador
    }
}

export default ScoreManager;
